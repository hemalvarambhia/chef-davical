server {
  listen 80;
  server_name <%= @configuration[:server_name]%>;

  access_log /var/log/nginx/davical_access.log;
  error_log /var/log/nginx/davical_error.log;

  location / {
    rewrite ^/principals/users/(.+)$    http://ical.example.com/caldav.php/$1 break;
  }
}